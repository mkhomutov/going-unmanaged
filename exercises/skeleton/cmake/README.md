Find-modules go here — `FindHostSDK.cmake` in Chapter 40's shape — and the
root `CMakeLists.txt` adds this directory to `CMAKE_MODULE_PATH` the day the
first one arrives. Empty until then, on purpose.
