// The two normalizers of Recipe 50's demonstration, declared for main().
// Deliberately tiny: what the recipe is about is what is NOT here - the
// clamp_to_range each .cpp keeps to itself, which no header can name.
#pragma once

int normalize_reading(int raw);
int other_normalize_reading(int raw);
